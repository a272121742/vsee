import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';

<Meta title="框架指引|组件/表格" />

# 表格列配置器

**表格列配置器材**（Table Columns Provider）位于组件列表的`@comp/table/ColProvider.vue`文件中。

## 创造缘起

我们需要这样一个插件

这里是`Markdown`的相关文档。

## 构建组件

让我们来写一些例子。
