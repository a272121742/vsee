/* eslint-disable camelcase */

const vin = {
  dataIndex: 'vin',
  scopedSlots: { customRender: 'vin' },
  width: 160,
  align: 'center',
  fixed: 'left',
};
const vhcl_model = {
  dataIndex: 'vhcl_model',
  scopedSlots: { customRender: 'vhcl_model' },
  width: 160,
  align: 'center',
  fixed: 'left',
};
const vhcl_nm = {
  dataIndex: 'vhcl_nm',
  scopedSlots: { customRender: 'vhcl_nm' },
  width: 100,
  align: 'center',
};
const vhcl_brand = {
  dataIndex: 'vhcl_brand',
  scopedSlots: { customRender: 'vhcl_brand' },
  width: 100,
  align: 'center',
};
const vhcl_status = {
  dataIndex: 'vhcl_status',
  scopedSlots: { customRender: 'vhcl_status' },
  width: 100,
  align: 'center',
};
const vhcl_type = {
  dataIndex: 'vhcl_type',
  scopedSlots: { customRender: 'vhcl_type' },
  width: 100,
  align: 'center',
};
const vhcl_id_no = {
  dataIndex: 'vhcl_id_no',
  scopedSlots: { customRender: 'vhcl_id_no' },
  width: 160,
  align: 'center',
};
const cert_no = {
  dataIndex: 'cert_no',
  scopedSlots: { customRender: 'cert_no' },
  width: 160,
  align: 'center',
};
const upload_dt = {
  dataIndex: 'upload_dt',
  scopedSlots: { customRender: 'upload_dt' },
  width: 160,
  align: 'center',
};

const made_enterprise_nm = {
  dataIndex: 'made_enterprise_nm',
  scopedSlots: { customRender: 'made_enterprise_nm' },
  width: 200,
  align: 'center',
};

const annc_model = {
  dataIndex: 'annc_model',
  scopedSlots: { customRender: 'annc_model' },
  width: 150,
  align: 'center',
};
const body_colour = {
  dataIndex: 'body_colour',
  scopedSlots: { customRender: 'body_colour' },
  width: 100,
  align: 'center',
};

const chassis_cert_no = {
  dataIndex: 'chassis_cert_no',
  scopedSlots: { customRender: 'chassis_cert_no' },
  width: 160,
  align: 'center',
};
const chassis_id = {
  dataIndex: 'chassis_id',
  scopedSlots: { customRender: 'chassis_id' },
  width: 100,
  align: 'center',
};
const chassis_model = {
  dataIndex: 'chassis_model',
  scopedSlots: { customRender: 'chassis_model' },
  width: 100,
  align: 'center',
};
const ele_motor_model = {
  dataIndex: 'ele_motor_model',
  scopedSlots: { customRender: 'ele_motor_model' },
  width: 100,
  align: 'center',
};
const configure_serial_no = {
  dataIndex: 'configure_serial_no',
  scopedSlots: { customRender: 'configure_serial_no' },
  width: 160,
  align: 'center',
};
const ele_motor_no = {
  dataIndex: 'ele_motor_no',
  scopedSlots: { customRender: 'ele_motor_no' },
  width: 160,
  align: 'center',
};
const fuel_type = {
  dataIndex: 'fuel_type',
  scopedSlots: { customRender: 'fuel_type' },
  width: 100,
  align: 'center',
};
const emission_std = {
  dataIndex: 'emission_std',
  scopedSlots: { customRender: 'emission_std' },
  width: 100,
  align: 'center',
};
const engine_dspl = {
  dataIndex: 'engine_dspl',
  scopedSlots: { customRender: 'engine_dspl' },
  width: 100,
  align: 'center',
};
const power = {
  dataIndex: 'power',
  scopedSlots: { customRender: 'power' },
  width: 100,
  align: 'center',
};
const turnaround_mode = {
  dataIndex: 'turnaround_mode',
  scopedSlots: { customRender: 'turnaround_mode' },
  width: 100,
  align: 'center',
};
const front_track = {
  dataIndex: 'front_track',
  scopedSlots: { customRender: 'front_track' },
  width: 100,
  align: 'center',
};
const rear_track = {
  dataIndex: 'rear_track',
  scopedSlots: { customRender: 'rear_track' },
  width: 100,
  align: 'center',
};
const tyre_num = {
  dataIndex: 'tyre_num',
  scopedSlots: { customRender: 'tyre_num' },
  width: 100,
  align: 'center',
};
const tyre_size = {
  dataIndex: 'tyre_size',
  scopedSlots: { customRender: 'tyre_size' },
  width: 100,
  align: 'center',
};
const leaf_springs_num = {
  dataIndex: 'leaf_springs_num',
  scopedSlots: { customRender: 'leaf_springs_num' },
  width: 100,
  align: 'center',
};
const wheel_base = {
  dataIndex: 'wheel_base',
  scopedSlots: { customRender: 'wheel_base' },
  width: 100,
  align: 'center',
};
const axle_load = {
  dataIndex: 'axle_load',
  scopedSlots: { customRender: 'axle_load' },
  width: 100,
  align: 'center',
};
const axle_num = {
  dataIndex: 'axle_num',
  scopedSlots: { customRender: 'axle_num' },
  width: 100,
  align: 'center',
};
const steer_axle_num = {
  dataIndex: 'steer_axle_num',
  scopedSlots: { customRender: 'steer_axle_num' },
  width: 100,
  align: 'center',
};
const veranda_length = {
  dataIndex: 'veranda_length',
  scopedSlots: { customRender: 'veranda_length' },
  width: 100,
  align: 'center',
};
const veranda_width = {
  dataIndex: 'veranda_width',
  scopedSlots: { customRender: 'veranda_width' },
  width: 100,
  align: 'center',
};
const veranda_height = {
  dataIndex: 'veranda_height',
  scopedSlots: { customRender: 'veranda_height' },
  width: 100,
  align: 'center',
};
const inner_length = {
  dataIndex: 'inner_length',
  scopedSlots: { customRender: 'inner_length' },
  width: 100,
  align: 'center',
};
const inner_width = {
  dataIndex: 'inner_width',
  scopedSlots: { customRender: 'inner_width' },
  width: 100,
  align: 'center',
};
const inner_height = {
  dataIndex: 'inner_height',
  scopedSlots: { customRender: 'inner_height' },
  width: 100,
  align: 'center',
};
const total_mass = {
  dataIndex: 'total_mass',
  scopedSlots: { customRender: 'total_mass' },
  width: 100,
  align: 'center',
};
const rated_load_mass = {
  dataIndex: 'rated_load_mass',
  scopedSlots: { customRender: 'rated_load_mass' },
  width: 100,
  align: 'center',
};
const curb_weight = {
  dataIndex: 'curb_weight',
  scopedSlots: { customRender: 'curb_weight' },
  width: 100,
  align: 'center',
};
const mass_utilization_coefficient = {
  dataIndex: 'mass_utilization_coefficient',
  scopedSlots: { customRender: 'mass_utilization_coefficient' },
  width: 150,
  align: 'center',
};
const tow_mass = {
  dataIndex: 'tow_mass',
  scopedSlots: { customRender: 'tow_mass' },
  width: 100,
  align: 'center',
};
const rated_seats = {
  dataIndex: 'rated_seats',
  scopedSlots: { customRender: 'rated_seats' },
  width: 100,
  align: 'center',
};
const max_saddle_mass = {
  dataIndex: 'max_saddle_mass',
  scopedSlots: { customRender: 'max_saddle_mass' },
  width: 250,
  align: 'center',
};
const cab_passenger_num = {
  dataIndex: 'cab_passenger_num',
  scopedSlots: { customRender: 'cab_passenger_num' },
  width: 150,
  align: 'center',
};
const front_brake_mthd = {
  dataIndex: 'front_brake_mthd',
  scopedSlots: { customRender: 'front_brake_mthd' },
  width: 150,
  align: 'center',
};
const rear_brake_mthd = {
  dataIndex: 'rear_brake_mthd',
  scopedSlots: { customRender: 'rear_brake_mthd' },
  width: 150,
  align: 'center',
};
const front_oprt_mthd = {
  dataIndex: 'front_oprt_mthd',
  scopedSlots: { customRender: 'front_oprt_mthd' },
  width: 150,
  align: 'center',
};
const rear_oprt_mthd = {
  dataIndex: 'rear_oprt_mthd',
  scopedSlots: { customRender: 'rear_oprt_mthd' },
  width: 150,
  align: 'center',
};
const max_speed = {
  dataIndex: 'max_speed',
  scopedSlots: { customRender: 'max_speed' },
  width: 100,
  align: 'center',
};
const oil_consumption = {
  dataIndex: 'oil_consumption',
  scopedSlots: { customRender: 'oil_consumption' },
  width: 100,
  align: 'center',
};
const only_ele_flg = {
  dataIndex: 'only_ele_flg',
  scopedSlots: { customRender: 'only_ele_flg' },
  width: 100,
  align: 'center',
};
const prod_dt = {
  dataIndex: 'prod_dt',
  scopedSlots: { customRender: 'prod_dt' },
  width: 160,
  align: 'center',
};
const issuance_dt = {
  dataIndex: 'issuance_dt',
  scopedSlots: { customRender: 'issuance_dt' },
  width: 160,
  align: 'center',
};
const remark = {
  dataIndex: 'remark',
  scopedSlots: { customRender: 'remark' },
  width: 200,
  align: 'center',
};
const enterprise_std = {
  dataIndex: 'enterprise_std',
  scopedSlots: { customRender: 'enterprise_std' },
  width: 150,
  align: 'center',
};
const mfrs_nm = {
  dataIndex: 'mfrs_nm',
  scopedSlots: { customRender: 'mfrs_nm' },
  width: 200,
  align: 'center',
};
const prdct_addr = {
  dataIndex: 'prdct_addr',
  scopedSlots: { customRender: 'prdct_addr' },
  width: 150,
  align: 'center',
};
const enterprise_other_info = {
  dataIndex: 'enterprise_other_info',
  scopedSlots: { customRender: 'enterprise_other_info' },
  width: 150,
  align: 'center',
};
const product_annc_cd = {
  dataIndex: 'product_annc_cd',
  scopedSlots: { customRender: 'product_annc_cd' },
  width: 150,
  align: 'center',
};
const annc_batch = {
  dataIndex: 'annc_batch',
  scopedSlots: { customRender: 'annc_batch' },
  width: 100,
  align: 'center',
};
const annc_valid_dt = {
  dataIndex: 'annc_valid_dt',
  scopedSlots: { customRender: 'annc_valid_dt' },
  width: 160,
  align: 'center',
};
const upload_user = {
  dataIndex: 'upload_user',
  scopedSlots: { customRender: 'upload_user' },
  width: 150,
  align: 'center',
};
const feedback_cd = {
  dataIndex: 'feedback_cd',
  scopedSlots: { customRender: 'feedback_cd' },
  width: 250,
  align: 'center',
};
const unique_print_cd = {
  dataIndex: 'unique_print_cd',
  scopedSlots: { customRender: 'unique_print_cd' },
  width: 300,
  align: 'center',
};
const paper_cd = {
  dataIndex: 'paper_cd',
  scopedSlots: { customRender: 'paper_cd' },
  width: 100,
  align: 'center',
};
const is_no_tax = {
  dataIndex: 'is_no_tax',
  scopedSlots: { customRender: 'is_no_tax' },
  width: 200,
  align: 'center',
};
const create_dt = {
  dataIndex: 'create_dt',
  scopedSlots: { customRender: 'create_dt' },
  width: 160,
  align: 'center',
};
const update_dt = {
  dataIndex: 'update_dt',
  scopedSlots: { customRender: 'update_dt' },
  width: 160,
  align: 'center',
};
const check_digit = {
  dataIndex: 'check_digit',
  scopedSlots: { customRender: 'check_digit' },
  width: 150,
  align: 'center',
};
const data_status = {
  dataIndex: 'data_status',
  scopedSlots: { customRender: 'data_status' },
  width: 100,
  align: 'center',
};
const config_id = {
  dataIndex: 'config_id',
  scopedSlots: { customRender: 'config_id' },
  width: 100,
  align: 'center',
};

export default {};

// 过点信息
export const certifiColumns = [
  vin,
  vhcl_model,
  vhcl_nm,
  vhcl_brand,
  vhcl_status,
  vhcl_type,
  vhcl_id_no,
  cert_no,
  upload_dt,
  made_enterprise_nm,
  annc_model,
  body_colour,
  chassis_cert_no,
  chassis_id,
  chassis_model,
  ele_motor_model,
  configure_serial_no,
  ele_motor_no,
  fuel_type,
  emission_std,
  engine_dspl,
  power,
  turnaround_mode,
  front_track,
  rear_track,
  tyre_num,
  tyre_size,
  leaf_springs_num,
  wheel_base,
  axle_load,
  axle_num,
  steer_axle_num,
  veranda_length,
  veranda_width,
  veranda_height,
  inner_length,
  inner_width,
  inner_height,
  total_mass,
  rated_load_mass,
  curb_weight,
  mass_utilization_coefficient,
  tow_mass,
  rated_seats,
  max_saddle_mass,
  cab_passenger_num,
  front_brake_mthd,
  rear_brake_mthd,
  front_oprt_mthd,
  rear_oprt_mthd,
  max_speed,
  oil_consumption,
  only_ele_flg,
  prod_dt,
  issuance_dt,
  remark,
  enterprise_std,
  mfrs_nm,
  prdct_addr,
  enterprise_other_info,
  product_annc_cd,
  annc_batch,
  annc_valid_dt,
  upload_user,
  feedback_cd,
  unique_print_cd,
  paper_cd,
  is_no_tax,
  create_dt,
  update_dt,
  check_digit,
  data_status,
  config_id,

];
