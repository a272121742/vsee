import { Story, Preview, Meta, Props } from '@storybook/addon-docs/blocks';

<Meta title="组件库|Form表单/工作流选人封装" />

# 工作流 Workflow

计划提供工作流方面的数据选择：

1. 通过接口获取当前用户角色下的可用人员；
2. 如果没有人员，通过其他接口查询其他可用人员；（查询0-报错，查询1-直接显示并选择，查询多-提示进行选择）
3. 人员获得后保存到工作流的池子里；
4. 调用工作流更新数据；

这里无法制作成组件，只能把业务用函数式的方式进行包装，提供对应的配置项。